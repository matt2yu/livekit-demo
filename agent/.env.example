LIVEKIT_URL=
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=

# Supabase — where confirmed orders land, and what the admin dashboard reads.
# The SERVICE_ROLE key bypasses RLS and must never reach the frontend.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
